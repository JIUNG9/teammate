# Handover — alice — 2026-05-07

This file is a leaving artifact. It captures the team-relevant facts that lived in the departing engineer's personal `~/.claude/` memory. Hand it to your successor or fold the useful entries into the team brain (`teammate memory-import` on the receiver's machine).

- Memory root: `/Users/alice/.claude`
- Redaction: redacted
- Entries: 4

---

## Team rules — conventions every engineer should follow (1)

- we deploy via ArgoCD on every merge to main
  _source: `MEMORY.md` line 14_

## Team facts — services, owners, stack (2)

- Auth service owner is alice; on-call rotation in PagerDuty
  _source: `MEMORY.md` line 22_
- Production cluster runs Kubernetes 1.33; metrics live at db01.prod.internal:9090
  _source: `MEMORY.md` line 31_

## References — pointers to external resources (1)

- see Linear project AUTH-2026-Q2 for the migration plan
  _source: `MEMORY.md` line 47_

## Things you should know about how I worked

When the auth service goes red, your first move should be checking the
upstream identity provider — 60% of our incidents in the last year
trace there, not to our code. Bob keeps the runbook for that step,
and he's worth talking to in your first month. The script in
`scripts/auth-debug.sh` saved me hours; nobody else in the team uses it
and I never got around to documenting it properly. You should rewrite
the on-call rotation policy — we've grown past what the current
schedule can sustain, and I never pushed hard enough on that.

---

_Generated by `teammate memory-export`. If you spot something that shouldn't have left the building, redact it before you share the file. The exporter is read-only on `~/.claude/` — this artifact is the only thing it produces._
